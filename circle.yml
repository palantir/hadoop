machine:
  java:
    version: oraclejdk8
  environment:
    TERM: dumb

checkout:
  post:
    - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
    - echo "user=$BINTRAY_USERNAME" > .credentials
    - echo "password=$BINTRAY_PASSWORD" >> .credentials
    - echo "realm=Bintray API Realm" >> .credentials
    - echo "host=api.bintray.com" >> .credentials

dependencies:
  override:
    - wget http://www-eu.apache.org/dist/maven/maven-3/3.3.9/binaries/apache-maven-3.3.9-bin.tar.gz
    - sudo tar xfz apache-maven-3.3.9-bin.tar.gz -C /usr/local/
    - sudo ln -sf /usr/local/apache-maven-3.3.9/bin/mvn /usr/local/apache-maven/bin/mvn

compile:
  override:
    - echo ""

test:
  override:
    - echo ""

deployment:
  release:
    tag: /3.0.0-palantir.[0-9]+/
    commands:
      - ./palantir/publish.sh
  rc:
    tag: /3.0.0-palantir.[0-9]+-rc[0-9]+/
    commands:
      - ./palantir/publish.sh
